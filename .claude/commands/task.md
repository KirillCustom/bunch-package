---
description: Взять задачу mIOnika и выполнить её на модели из её тега
argument-hint: TSK-NN
---

Выполнить задачу `$ARGUMENTS` проекта bunch-package на той модели, которая
указана в её теге.

## Шаги

1. Прочитай задачу целиком: `get_atom($ARGUMENTS, projectId: "bunch-package")`,
   включая приписки, и посмотри связи `get_atom_neighbors`.

2. Возьми тег модели: `opus`, `sonnet`, `haiku` или `fable`.

   Тега нет — предложи по критериям из `.claude/rules/mionika.md`, поставь через
   `batch_crud: update_atom` (сохранив остальные теги) и скажи об этом одной
   строкой. Разрешения на очевидный случай не спрашивай.

3. Выбери исполнителя по тегу.

   - **`opus`** — **не делегировать**, вести в главном цикле. Здесь это почти
     всегда правки в разборе, применении или откате патчей: они кончаются
     корпусным прогоном, мутациями и развилками, на которых нужен разговор с
     пользователем.
   - **Тег совпадает с моделью текущей сессии** — выполняй сам.
   - **`sonnet` / `haiku` / `fable`, не совпавшие с сессией** — делегируй:

     ```
     Agent(
       subagent_type: "task-runner",
       model: "<тег>",
       description: "TSK-NN <кратко>",
       prompt: "Выполни TSK-NN проекта bunch-package. <суть задачи, что уже
                измерено, где лежит стенд, чем доказывать>"
     )
     ```

     В определении `task-runner` поле `model:` намеренно не объявлено, поэтому
     переданный тег — единственный источник модели.

4. **Проверь работу сам, прежде чем пересказывать отчёт.** Отчёт — заявка, а не
   доказательство; в первой же делегированной задаче четыре пропуска прошли
   шесть зелёных проверок CI и нашлись только руками (INS-12). Минимум:

   - забрать ветку и прогнать `bun test` и `bun run typecheck` у себя;
   - воспроизвести сценарии из задачи — те, что описаны в «как доказать», и
     отдельно отказы: они пишутся реже всего и ломаются незаметнее всего;
   - снять одну защиту мутацией и убедиться, что тест краснеет, — вместо того
     чтобы верить таблице мутаций в отчёте;
   - сверить каждое число из PR и README с атомом-источником в mIOnika:
     знаменатель теряется чаще, чем цифра;
   - проверить, что новая команда попала в справку CLI, README (строка, раздел,
     число команд в шапке), список команд `CLAUDE.md` и дымовое задание.

   Нашлось расхождение — вернуть агенту через `SendMessage` с воспроизведением,
   а не чинить молча: он в контексте задачи, и правка обойдётся дешевле.

5. Перескажи суть: что изменено, зелёные ли сюита и типы, какие мутации
   прогнаны, итог корпуса числами, что ты проверил сам, ветка и PR. Мерж без
   просьбы не делай.

## Границы

Корпусный прогон идёт около получаса и требует сети: если агент его не сделал,
это не «мелочь», а недоказанная правка — скажи об этом прямо.
